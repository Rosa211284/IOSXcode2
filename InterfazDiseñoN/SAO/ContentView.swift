
import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Kirito")
                .font(.system(size: 30, design: .serif))
            Text("________________________________")
            Imagen_centro()
            Reglas2()
        }
    }
}

#Preview {
    ContentView()
}
